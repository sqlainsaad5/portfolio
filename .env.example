# Optional: MongoDB Atlas connection string for dynamic project data via API
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/portfolio

# Secret for POST /api/seed (Bearer token) — only for one-time DB seeding
# SEED_SECRET=your-long-random-string

# Silent WhatsApp notify for feedback form (CallMeBot — free)
# 1) On WhatsApp, add +34 644 66 05 32 and send: I allow callmebot to send me messages
# 2) Bot replies with your apikey — paste it below
# CALLMEBOT_API_KEY=your-apikey
# WHATSAPP_NUMBER=923061975881
